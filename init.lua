return {
    colorscheme = "dracula",
    -- plugins = {
    --     {"nvim-neo-tree/neo-tree.nvim", config = function(plugin, opts) end}
    -- },
    polish = function()
        -- require("neo-tree").setup({
        --     filesystem = { filtered_items = { visible = true } }
        -- })
    end
}
